

export type ModelArgs = {
    id?: string,
    name: string,
    src: string,
    position: [number,number,number],
    scale: number,
    rotation: [number,number,number],
    info?: string,
    image?: string
}